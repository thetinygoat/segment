[package]
name = "segment"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = [
    "rt-multi-thread",
    "net",
    "io-util",
    "macros",
] }
redis-protocol = { version = "6.0.0", features = ["bytes"] }
bytes = "1.11.0"
anyhow = "1.0"
